## RSA 2048 驗證和 mbedTLS 開源測試問題

[未有直接 Source 錨點，待確認] 1. 解密後的資料確實有正確的 hash 值，但前面還有一些無意義的資料。
2. 在 debug 源碼後發現，解密是從最後一個 byte 開始進行的，並且源碼會將所有 0xff 的資料刪除，然後複製到另一個陣列。因此需要了解這些無意義資料是如何產生的。
3. 目前發現 `bn_mod_exp(c, m, e, edigits, n, ndigits)` 中的 `c` 與原始程式碼中生成的 `c` 不一致。原始的 `c` 陣列只有 7 個參數，而使用公鑰和簽章後得到的 `c` 有 12 個參數。
[未有直接 Source 錨點，待確認] 4. 原始的 `c` 陣列和 eToken 生成的 `c` 陣列不一致，但演算法看不太懂，仍在研究中。

1. 修改簽章中的某一個 byte，整個解密出來的 hash 都是錯的 - 表示簽章資料應該是正確的。
2. 修改公鑰中的某一個 byte，整個解密出來的 hash 都是錯的 - 表示公鑰資料應該是正確的。
3. 原始程式碼中的金鑰對用 bin 檔案的 hash 進行加密解密，產出的 hash 也是正確的 - 表示與 hash 長度無關。

- [未有直接 Source 錨點，待確認] 使用 OpenSSL 生成金鑰對，然後簽章，並將簽章和公鑰放到開源程式中測試。
   - 使用其他 eToken 生成的 bin 檔案再次驗證。
   - 查看 eToken 的 verify 函數 - 它被封裝在 DLL 中，無法查看源碼。
   - 使用 mbedTLS 進行測試。

### mbedTLS 測試結果
後來使用 mbedTLS 進行解密，解出的資料也是一樣，前面的資料應該是 ASN.1 格式。

[未有直接 Source 錨點，待確認] 使用網路上的 ASN.1 解析工具查看，確實解析出的結果是正確的。

- RSA2048 解密時間：1分56秒

[未有直接 Source 錨點，待確認] 1. 使用 OpenSSL 生成金鑰對進行簽章和驗證。
2. 確認 PD FW 的 code sign 功能是否正確。
3. 檢查 SHA256 和 RSA2048 的計算演算法是否正確。