<context_boundaries>
<core_topic id="hubdriverinstaller-user-guide" title="HubDriverInstaller User Guide" path="/driver/hubdriverinstaller-user-guide.html">

## Overview
HubDriverInstaller.exe is a command-line tool designed for installing and uninstalling device filter drivers. It features automatic detection for legacy or broken drivers, ensuring a clean and stable installation process.
---
## ⚙️Basic Usage
Execute the following syntax in the Command Prompt (cmd):
```c#
HubDriverInstaller.exe [command]
```
---
## 📋 Commands
---
## 💻 Examples
### 1. Uninstall Driver
Run the -u command to remove the current driver.
Command Input:
```c#
HubDriverInstaller.exe -u
```
Terminal Output Example:
```c#
GL Driver:2.4.0.0
Uninstalling device filter driver, please wait...
...............Uninstall driver done!
```
### 2. Install Driver
Run the -i command to install the new driver.
Command Input:
```c#
HubDriverInstaller.exe -i
```
Terminal Output Example:
```c#
Installing device filter driver, please wait...
.................
GL Driver:2.4.0.0
Install driver done!
```

</core_topic>
  <related_context>
    <related id="hp-end-user-tool-packing-user-guide" title="HP End User Tool Packing User Guide" path="/tools/hp-end-user-tool-packing-user-guide.html">
    
    Install Shield Sample script 
    ### 下面為Yihsun修正過文件，以下面文件為標準
    第一代Monitor 文件，有 hub，Billboard , Scaler , PD
    code sign版本，後面版本基本上都是以此版本為基準
    Reader 版的Monitor 文件，有 Billboard , Scaler , PD
    提供給Quest HP EndUser Tool Sample Code 的 sepc  & Sample Code
    PackageForTheWeb400 安裝檔
    給客戶的Release Note
    
    </related>
    <related id="etoken-system" title="Etoken System" path="/code-sign/etoken-system.html">
    
    ### Server
    > **Note:** 使用 Proxmox 登入
    https://172.17.50.157:8006/#v1:0:=node%2Fgli-pve02:4:10:=contentIso:::37::2
    User name:glifw
    Password:Gene@fwfw
    > **Note:** 
    > **Note:** ［eToken Server v4.0］（現行版本）
    ---
    ### eToken System Server
    - 檔案位置
    - 安裝手冊
    - 資料庫說明
    ---
    ### eToken Server
    回應 eTokenClient Tool 的需求，並提供帳號密碼機制管理登入權限
    - 注意事項
    ---
    ### eToken Client
    提供GUI版本Tool ，可以傳送 command 給 eTokenServer  Tool 來做 generate key & sign 功能 ，並傳送 sign 後的 hub bin file 回來。
    注意：v2.0.0.2 的版本，要 sign 的檔案需與 exe 放在同資料夾下。
    - 檔案位置
    - 現行版本
    - 手冊
    - 參考資料
    ---
    
    </related>
    <related id="etoken-安全簽章系統技術說明" title="eToken 安全簽章系統技術說明" path="/code-sign/etoken-安全簽章系統技術說明.html">
    
    ## 1. 系統概述
    本文件旨在說明 eToken 系統如何從一個原始的二進位檔案 (ori bin) 開始，經過整合、簽章與封裝等一系列步驟，最終產生安全的 sig.bin 與 rom 檔案。整個流程由 eToken Server 作為協調者，與 Dongle Server (安全核心) 和 GLBin (檔案處理工具) 協同運作完成。
    ---
    ## 2. 系統架構圖
    此架構圖展示了系統中各個元件的部署位置及其相互之間的通訊關係。
    ---
    ## 3. 詳細處理流程
    整個流程可分為三個主要階段：
    ---
    ## 4. 流程視覺化圖表
    4.1 循序圖 (Sequence Diagram)
    4.2 流程圖 (Flowchart)
    ---
    ## 5. 演算法特定規則
    在階段三 (最終檔案生成) 中，GLBin 組裝檔案的格式會根據簽章演算法 (ECDSA/RSA/RSA + HID) 而有所不同。
    ---
    ## 6. 各晶片韌體格式詳解
    ---
    ## 7. 晶片格式總覽表
    ## GLBinTool Commands
    ---
    ### ⚙️ WIP 階段
    ```c
    glbin --wip -i Fw.bin [--input# Fw#.bin] -p publickey.bin -t <rsa2048|ecdsa256>
    ```
    用途
    將 Public Key 嵌入原始 Bin，依晶片規則重算 checksum、產生 WIP 檔
    在 case B 時，每個 input 都會獨立生成一個 WIP 檔，其餘 case 皆只會生成一個合併的 WIP 檔
    輸出檔名為原始檔名末  .bin 改為 wip.bin ，例如 FW.bin 改為  FWwip.bin 
    必要參數 
    - i, --input → 原始 Bin 檔
    - p, --publickey → Public Key 檔案
    - -t → 簽章類型（rsa2048 / ecdsa256）
    可選參數
    - --input#→ 第 N 個原始 Bin 檔
    回傳
    -  回傳 Output 檔案數量，若為 0 則為 Fail
    特殊狀況
    -  case A in 才需要放 public key 進去
    ---
    ### 🔑 SIGN 階段
    ```c
    glbin --sign -i Fw.bin [--input# Fw#.bin] --g signature.bin [--signature# signature#.bin] -o sig.bin -p publickey.bin  -t <rsa2048|ecdsa256> [-f]
    ```
    用途
    計算 wip.bin 的 Hash，產生 Signed bin 檔。
    必要參數
    - i, --input → wip.bin 檔
    - g, --signagure —> Signature 檔案
    - o, --output → 輸出 sig 檔
    - p, --publickey → Public Key 檔案
    - -t → 簽章類型（rsa2048 / ecdsa256）
    可選參數
    - --input#→ 第 N 個 wip.Bin 檔
    - --signagure# —>第 N 個  Signature 檔案
    - f, --Convert hid code sign bin file format —> Convert hid code sign bin file format
    特殊狀況
    -  case B in 除了把 public key & hash &signature 放到 bin檔內外也需要放到bin檔的結尾
    ---
    ### 📦 ROM 階段
    ```c
    glbin --rom -i Fw.bin [--input# Fw#.bin] -g signature.bin [--signature# signature#.bin] -o rom.bin -p publickey.bin-t <rsa2048|ecdsa256>
    ```
    用途
    依晶片演算法，組裝最終檔案 rom。
    必要參數
    - i, --input → wip.bin 檔
    - g, --signagure —> Signature 檔案
    - o, --output → 輸出檔案
    - p, --publickey → Public Key 檔案
    - -t → 簽章類型（rsa2048 / ecdsa256）
    可選參數
    - --input#→ 第 N 個 wip.Bin 檔
    - --signagure# —>第 N 個  Signature 檔案
    ---
    ## 測試Chip & bin 檔
    
    </related>
  </related_context>
</context_boundaries>